[DOCS ONLY]
You are an expert Ruby on Rails Engineer and Architect. You write idiomatic, maintainable Rails code, following best practices (such as thin controllers, domain-driven naming, and robust testing).

Context & Goal
I have a technical challenge based on an open Redmine issue: Feature #43881: Strengthen API authentication.

Here are the exact requirements from the challenge description (Technical Task.pdf): <challenge_requirements> Redmine is an open-source project management app on Ruby on Rails — github.com/redmine/redmine. The task is a real open ticket from its issue tracker. Read Redmine #43881 first. It asks for several improvements — personal access tokens with expiration, scopes, rate limiting, audit logging, endpoint control, and CORS. The poster acknowledges the scope is large, proposes a phased rollout, and is still waiting on maintainer guidance. There is no estimated-hours field.

The full ticket is more than ~2 hours of work. You're not expected to cover all of it — choose what to build and ship that.

Required core — rate limiting Your slice must implement working API rate limiting (pillar 3 of the ticket): a caller that goes over the limit gets an HTTP 429. The other pillars — tokens, scopes, audit logging, endpoint control, CORS — are optional; scope them around the core. How you rate-limit, and the trade-offs of that choice, are yours to decide and defend in the README.

What to deliver

Git repository: Fork or new repo from github.com/redmine/redmine, branch off tag 6.1.2. We should be able to clone, install, run migrations, and verify your slice. How you verify it — and what level of testing the slice needs — is your call.
AI workflow artifacts: This is an AI-first role, so show your work.
README: Brief. Cover approach, what's done, what's deferred, assumptions made, and how to run and verify. For the rate-limiting core, also state how it works and the limits of your approach.
Time: ~2hrs guideline. Aim for a focused slice, not a sprawling implementation. A small slice with strong artifacts beats a half-finished large one. Rate limiting is the required core; treat the rest as scope you trade against your time. </challenge_requirements>

Task
Your task is to analyze these requirements and the Redmine issue (#43881), and produce the following:

Feature Specification

Define the exact scope of the "defensible slice" we will build within a 2-hour budget.
Describe the chosen rate-limiting strategy (e.g., using Rack::Attack, custom middleware, Redis vs MemoryStore) and the trade-offs.
Detail any optional features we will include (if any) or explicitly document them as deferred.
Task Decomposition

Break down the spec into small, actionable steps (e.g., Repo setup/branching off 6.1.2, Rate limiting core implementation, Testing, README writing).
Provide time allocations for each step to ensure we fit within the ~2-hour budget.
Implementation Plan

Provide a step-by-step technical implementation plan.
Specify which files will be created/modified.
Outline the testing strategy (e.g., Minitest/RSpec integration tests to verify the HTTP 429 response).
Provide an outline for the final README.md defending our trade-offs.
Instructions: Read the Redmine issue #43881 for context. Then, output the Feature Specification, Task Decomposition, and Implementation Plan. Do not start writing the code until I approve the plan.
Task docs/Technical Task.pdf
Issue https://www.redmine.org/issues/43881
redmine source code  redmine

Create spec folder in docs and save md files there separate md files for every type of docs and add readme.md

Dont start the implemetation until i aproove the plan